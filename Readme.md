Parameters expected in the program

 /**
* @param args the command line arguments
* args[0] = column count
* args[1] = rows count
* args[2] = p for print the grid, s for search for largest continuous color block
*/